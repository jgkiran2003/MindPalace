# MindPalace Phase 1 does not currently load configuration from environment
# variables. Runtime settings are defined in config/settings.toml.
#
# The default local Ollama endpoint is:
#   http://localhost:11434
#
# To change the Ollama host, embedding model, storage paths, or retrieval
# settings, edit config/settings.toml.

